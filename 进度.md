# 项目进度

## 当前任务：修复 admin 登录页 "An internal error occurred" 错误

### 问题描述
访问 https://mt-worker.linkact.top/admin 登录时，提交用户名/密码后页面显示 "An internal error occurred"。

### 诊断结果

**已完成的分析：**

1. **浏览器测试（chrome-runner agent）**：
   - `GET /admin` → 200 OK，登录页面正常渲染
   - `GET /_matrix/client/v3/login` → 200 OK，返回支持的登录方式
   - `POST /_matrix/client/v3/login` → 500，响应 `{"errcode":"M_UNKNOWN","error":"An internal error occurred"}`
   - `GET /auth/oidc/providers` → 500，同上错误
   - `GET /admin/api/bootstrap-status` → 500，同上错误

2. **根因定位**：
   - D1 数据库 `matrix-worker-db` 中的表不存在，导致所有数据库查询抛出异常
   - 全局错误处理器（`src/index.ts:429-435`）捕获异常后返回通用 500 错误
   - **两个具体原因**：
     - `package.json` 迁移命令引用 `tuwunel-db`（旧项目名），但实际数据库是 `matrix-worker-db`
     - 编号迁移文件（002-015）从未被包含在自动迁移流程中
   - `005_idp_providers.sql` 中的 `idp_providers` 表不在 `schema.sql` 中

3. **API 端点分析**：
   - 登录 POST 流程：`POST /_matrix/client/v3/login` → `verifyPassword()` → `getPasswordHash()` → 查询 `users` 表 → 表不存在 → 异常 → 500
   - OIDC providers：查询 `idp_providers` 表 → 表不存在 → 异常 → 500
   - Bootstrap status：AdminDO 查询 `users` 表 → 表不存在 → 异常 → 500

### 已完成的修复

| 文件 | 修改内容 |
|------|----------|
| `package.json` | 修复 `db:migrate` 和 `db:migrate:local` 命令：数据库名从 `tuwunel-db` 改为 `matrix-worker-db`，并追加了所有 15 个编号迁移（002-015） |
| `migrations/schema.sql` | 末尾添加了 `idp_providers`、`idp_user_links`、`server_config` 三个关键表（原只在编号迁移中定义） |

### 待执行操作（需要用户手动完成）

由于本地环境未配置 `CLOUDFLARE_API_TOKEN`，以下操作需要用户在已认证的环境中执行：

```bash
# 1. 应用所有数据库迁移（创建所有表）
npm run db:migrate

# 2. 设置管理员账号 secrets
npx wrangler secret put ADMIN_USERNAME
npx wrangler secret put ADMIN_PASSWORD

# 3. 重新部署
npm run deploy
```

**注意**：运行前需确保已通过 `npx wrangler login` 登录或设置了 `CLOUDFLARE_API_TOKEN` 环境变量。

### 其他发现（次要问题）

- `wrangler.jsonc` 中 `SERVER_NAME` 设置为 `"matrix-worker"`（非完整域名），而实际域名为 `mt-worker.linkact.top`。这不会导致登录失败，但可能影响联邦功能。建议将 `SERVER_NAME` 改为 `"mt-worker.linkact.top"`。
- 多个 KV namespace 绑定了同一个 ID (`77da1c7cbd6243f8b69647eb4dffd669`)，需要确认是否符合预期。
- `GET /favicon.ico` 返回 404（无影响，仅浏览器控制台警告）

### 关键文件路径

| 文件 | 用途 |
|------|------|
| `src/index.ts` | 入口文件，全局中间件和错误处理器（429-435行） |
| `src/api/login.ts` | 登录 API（POST `/_matrix/client/v3/login`） |
| `src/api/oidc-auth.ts` | OIDC 认证 API（GET `/auth/oidc/providers`） |
| `src/api/admin.ts` | Admin API 路由 |
| `src/admin/dashboard.ts` | Admin 前端 SPA（4223行，包含 HTML/CSS/JS） |
| `src/services/database.ts` | 数据库查询函数（`getPasswordHash` 等） |
| `src/utils/bootstrap.ts` | Bootstrap admin 账号逻辑 |
| `src/durable-objects/AdminDurableObject.ts` | Admin DO（Bootstrap 状态和配置管理） |
| `migrations/schema.sql` | 核心数据库 schema（21 个基础表 + 本次新增 3 个） |
| `migrations/005_idp_providers.sql` | IdP 表定义 |
| `wrangler.jsonc` | Cloudflare 部署配置 |
| `package.json` | 项目脚本（迁移命令已修复） |

### 登录流程参考

1. 用户访问 `/admin` → 返回 `dashboard.ts` 生成的 HTML 页面
2. 页面加载时调用 `GET /auth/oidc/providers` 加载 SSO 按钮（静默失败不影响登录）
3. 用户提交表单 → `POST /_matrix/client/v3/login`（`m.login.password` 类型）
4. 服务端验证密码 → 生成 access_token → 存入 D1 `access_tokens` 表
5. 后续 admin API 请求通过 `requireAuth()` + `requireAdmin` 中间件验证
